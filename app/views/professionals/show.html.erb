<p id="notice"><%= notice %></p>

<p>
  <strong><%=t("activerecord.attributes.professional.name")%>:</strong>
  <%= @professional.name %>
</p>

<%= generate_edit_button if can? :update, Professional %>
<%= button_to t('appointments_list'), professional_appointments_path(@professional), button_options('info') if can? :read, Appointment %>
<%= generate_back_button %>