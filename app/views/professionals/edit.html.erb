<h1><%= t("edit") %> <%= t("activerecord.models.professional") %></h1>

<%= render 'form', professional: @professional %>

<%= generate_show_button if can? :read, Professional %>
<%= generate_back_button %>